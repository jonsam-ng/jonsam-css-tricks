
import React from "react";
import { ColorfulButtonProps } from "./ColorfulButton.types";
import "./ColorfulButton.scss";
const ColorfulButton: React.FC<ColorfulButtonProps> = ({  }) => (
  <div className="colorful__button--container">
    <span>submit</span>
  </div>
);

export default ColorfulButton;
